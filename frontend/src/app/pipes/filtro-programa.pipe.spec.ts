import { FiltroProgramaPipe } from './filtro-programa.pipe';

describe('FiltroProgramaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProgramaPipe();
    expect(pipe).toBeTruthy();
  });
});
